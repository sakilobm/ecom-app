import React from 'react';
import styled from 'styled-components';
import { TouchableOpacity, Animated, Dimensions, View, ScrollView, StatusBar, StyleSheet } from 'react-native';
import { GradientColour } from './StyledComponents'
import { BlurView } from 'expo-blur';
class HomeScreen extends React.Component {

    render() {
        return (

            <BlurCard>

            </BlurCard>
        )
    }
}